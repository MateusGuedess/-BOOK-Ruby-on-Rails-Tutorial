<div id="<%= dom_id user %>">
  <p>
    <strong>Name:</strong>
    <%= user.name %>
  </p>

  <p>
    <strong>Email:</strong>
    <%= user.email %>
  </p>

  <h2>Microposts</h2>
  <% if user.microposts&.any? %>
    <%= user.microposts.first.content %>
  <% end %>



</div>
